return {
  -- LazyVim/Mason will install clangd automatically.
  -- clangd picks up compile_commands.json from your build dir
  -- (e.g. cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON .. ; ln -s build/compile_commands.json .)
}
